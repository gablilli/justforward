set daemon 60
set no syslog
set no bouncemail
set invisible
set logfile /var/log/fetchmail.log

poll pop.provider.com
  protocol POP3
  port 995
  ssl
  user "mail@example.org"
  password "yourpassword"
  fetchall
  keep
  mda "/usr/bin/msmtp --read-envelope-from --read-recipients -t"
